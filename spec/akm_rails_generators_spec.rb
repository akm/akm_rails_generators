require 'spec_helper'

describe AkmRailsGenerators do
  it 'has a version number' do
    expect(AkmRailsGenerators::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
